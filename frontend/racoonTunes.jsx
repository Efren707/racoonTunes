import React from "react";
import ReactDOM from "react-dom";

document.addEventListener("DOMContentLoaded", () => {
    const root = document.getElementById("root");
    debugger
    ReactDOM.render(<h1>Welcome to RacoonTunes!</h1>, root);
});